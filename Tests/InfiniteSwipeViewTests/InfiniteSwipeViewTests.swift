import XCTest
@testable import InfiniteSwipeView

final class InfiniteSwipeViewTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(InfiniteSwipeView().text, "Hello, World!")
    }
}
